import fs from 'fs'; import path from 'path'; const repoRoot=path.resolve(process.cwd(),'..'); const contentSrc=path.join(repoRoot,'content','sudoku'); const outDir=path.join(process.cwd(),'out','content','sudoku'); if(!fs.existsSync(contentSrc)){console.log('No content to copy:',contentSrc);process.exit(0)}; fs.mkdirSync(outDir,{recursive:true}); const copyRecursive=(src,dest)=>{const stat=fs.statSync(src); if(stat.isDirectory()){fs.mkdirSync(dest,{recursive:true}); for(const e of fs.readdirSync(src)){copyRecursive(path.join(src,e),path.join(dest,e))}} else {fs.copyFileSync(src,dest)}}; copyRecursive(contentSrc,outDir); console.log('Copied sudoku content into export:', outDir);
